from setuptools import setup

setup(
    name='bbox',
    version='0.1.0',
    description='A Python library for handling the 2D bounding box.',
    author='Pandede',
    install_requires=[
        "pydantic"
    ]
)
